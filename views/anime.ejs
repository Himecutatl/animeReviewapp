i like dicks

<!-- <section id="show-page">
  <div>Title: </div>
  <div><%= anime.title %></div>
  <div>Synopsis: </div>
  <div><%= anime.synopsis %></div>
  <div>Episodes: </div>
  <div><%= anime.episodes %></div>
  <div>Preview: </div>
  <div><%= anime.image_url %></div>
  
<% if (anime.reviews.length) { %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Review</th>
        <th>Rating</th>
      </tr>
    </thead>
    <tbody>
      <% anime.reviews.forEach(function(r) { %>
        <tr>
          <td><%= r.createdAt.toLocaleDateString() %></td>
          <td><%= r.content %></td>
          <td><%= r.rating %></td>
          <td><form method ="DELETE" action="/movies/<%= movie._id %>/reviews/<% r._id%>?">
            <button type="submit">Delete</button>
          </form></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
<% } else { %>
  <h5>No Reviews Yet</h5>
<% } %>


<!-- LEAVE A REVIEW -->

<!-- <br><br><h2>Reviews</h2>
<form id="add-review-form" method="POST"
  action="/movies/<%= anime._id %>/reviews"> 
  <label>Review:</label>
  <textarea name="content"></textarea>
  <label>Rating:</label>
  <select name="rating">
    <option value="1">1</option>
    <option value="2">2</option>
    <option value="3">3</option>
    <option value="4">4</option>
    <option value="5">5</option>
  </select>
  <input type="submit" value="Add Review">
</form> --> -->
